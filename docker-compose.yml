services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      - FRONTEND_URL=https://${DOMAIN}
      - GOOGLE_REDIRECT_URI=https://${DOMAIN}/api/auth/callback
      - DATABASE_URL=sqlite+aiosqlite:///./data/gtool.db
      - ERROR_BOT_URL=http://error-bot:8001
    volumes:
      - db-data:/app/data
    command: >
      uv run uvicorn app.main:app
      --host 0.0.0.0 --port 8000
    expose:
      - "8000"
    depends_on:
      - error-bot

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: https://${DOMAIN}/api
    restart: unless-stopped
    expose:
      - "3000"

  error-bot:
    build: ./bot
    restart: unless-stopped
    env_file: ./bot/.env
    expose:
      - "8001"
    volumes:
      - ./backend/app:/workspace/source/backend/app:ro
      - ./frontend/src:/workspace/source/frontend/src:ro
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    environment:
      - DOMAIN=${DOMAIN}
    depends_on:
      - backend
      - frontend

volumes:
  db-data:
  caddy-data:
  caddy-config:
